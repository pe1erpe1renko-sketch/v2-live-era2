import { SectionLabel } from "@/components/SectionLabel";

const LEFT = [
  "Оживить фото — значит превратить неподвижный снимок в короткое видео, где человек моргает, улыбается и поворачивает голову. Нейросети не нужны маркеры или ручная анимация: она сама разбирает кадр и решает, что в нём должно двигаться. Тем же способом оживает картинка, иллюстрация или детский рисунок — меняется только модель под задачу.",
  "Live Era2 работает прямо в браузере, на русском языке, без VPN и зарубежных карт. Устанавливать ничего не нужно ни на компьютере, ни на телефоне. Анимация занимает одну–три минуты, а на выходе получается обычный MP4 без водяных знаков — его можно скачать, отправить в мессенджер или выложить в ленту.",
];

const RIGHT = [
  "Называют это по-разному: нейросеть, нейронка, ИИ или AI. Суть одна — движущееся фото делает модель, которой хватает единственного кадра. И нужно это не только для соцсетей. Отдельный пласт задач — семейный архив: ожившие карточки прабабушек, реставрация повреждённых отпечатков, видео к юбилею из снимка, который человек не доставал десятилетиями.",
  "Искусственный интеллект здесь не заменяет память, а возвращает движение тому, что от неё осталось. Первая генерация бесплатна и не требует привязки карты — попробовать можно на любом снимке из вашего альбома.",
];

export function AboutText() {
  return (
    <section className="border-b border-rule">
      <div className="mx-auto max-w-[1440px] px-8 py-8 md:py-16 lg:px-16">
        <SectionLabel>О сервисе</SectionLabel>

        <h2 className="mt-4 text-[clamp(28px,3vw,44px)] font-light leading-[1.1] tracking-[-0.04em] text-ink">
          Оживление фотографий нейросетью
        </h2>

        <div className="mt-8 grid gap-12 md:grid-cols-2">
          {[LEFT, RIGHT].map((col, i) => (
            <div key={i} className="flex max-w-[520px] flex-col gap-4">
              {col.map((p) => (
                <p key={p} className="text-[14px] leading-[1.7] text-ink2">
                  {p}
                </p>
              ))}
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
